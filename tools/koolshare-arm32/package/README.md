# cloudflared koolshare package

- `scripts/install.sh`: 安装 cloudflared 到 `/koolshare/bin`
- `scripts/start.sh`: 按 `/koolshare/configs/cloudflared/config.yml` 启动 tunnel
- `scripts/stop.sh`: 停止 tunnel
- `scripts/uninstall.sh`: 卸载二进制

请先把 Tunnel credentials 文件放到 `/koolshare/configs/cloudflared/`。
